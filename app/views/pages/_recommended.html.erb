<div id="recommended-p-container" class="card-grid">
  <% @top_places.take(5).each do |place| %>
    <div class="card">
      <div class="card-body">
        <p class="card-text"><%= place.address %></p>
      </div>
    </div>
  <% end %>
</div>
